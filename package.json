{
  "name": "@tfg/root",
  "version": "0.0.0",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "client/*/*",
    "infra/*/*"
  ],
  "scripts": {
    "start": "run-p watch serve",
    "watch": "tfg build -w",
    "serve": "tfg serve -p dev",
    "mock": "json-server db.json",
    "build:all": "tfg build -a",
    "build:libs": "tfg build -l",
    "prepare": "tfg build -a"
  },
  "devDependencies": {
    "@tfg-commands/cli": "0.0.0",
    "json-server": "0.16.1",
    "lerna": "3.20.2",
    "npm-run-all": "4.1.5"
  }
}
