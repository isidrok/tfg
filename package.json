{
  "name": "@tfg/root",
  "version": "0.0.0",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "client/*/*",
    "infra/*/*"
  ],
  "scripts": {
    "start": "yarn watch & yarn serve",
    "watch": "tfg build -w",
    "serve": "tfg serve -p dev",
    "build:all": "tfg build -a",
    "build:libs": "tfg build -l",
    "prepare": "tfg build -l -p @tfg-core/bootstrap"
  },
  "dependencies": {
    "@tfg-commands/cli": "0.0.0",
    "lerna": "3.20.2"
  }
}
